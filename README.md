# DHLSONIC

This project aims to help manage the `Sonic` sector of `DHL`.

## Status

**In development**

All changes are documented in the [`CHANGELOG.md`](./CHANGELOG.md) file.

## Requirements

- Python 3.12

All project dependencies are listed in the `requirements.txt` file.  
To set up the environment:

```bash
python3 -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
